{
  "pluginAlias": "WinixPurifiers",
  "pluginType": "platform",
  "singular": true,
  "customUi": true,
  "customUiPath": "./dist/homebridge-ui",
  "schema": {
    "type": "object",
    "properties": {
      "exposeAirQuality": {
        "title": "Air Quality Sensor",
        "description": "Whether or not to expose the air quality sensors to HomeKit",
        "type": "boolean",
        "default": false
      },
      "exposeAmbientLight": {
        "title": "Ambient Light Sensor",
        "description": "Whether or not to expose the ambient light sensors to HomeKit",
        "type": "boolean",
        "default": false
      },
      "exposePlasmawave": {
        "title": "Plasmawave Switch",
        "description": "Whether or not to expose switches for Plasmawave on/off",
        "type": "boolean",
        "default": false
      },
      "exposeAutoSwitch": {
        "title": "Auto Switch",
        "description": "Whether or not to expose switches for Auto mode on/off",
        "type": "boolean",
        "default": false
      },
      "cacheIntervalSeconds": {
        "title": "Winix Response Cache Interval (seconds)",
        "description": "Time, in seconds, for how long to reuse cached responses from Winix",
        "type": "integer",
        "default": 60,
        "minimum": 1,
        "required": true
      },
      "deviceRefreshIntervalSeconds": {
        "title": "Device Refresh Interval (seconds)",
        "description": "Time, in seconds, for how often to poll Winix to refresh the device list. You can always just restart Homebridge to refresh the device list",
        "type": "integer",
        "default": 300,
        "minimum": 10,
        "required": true
      },
      "auth": {
        "type": "object",
        "title": "Authentication",
        "properties": {
          "username": {
            "type": "string",
            "title": "Username",
            "description": "Your Winix account username (email)",
            "readonly": true
          },
          "userId": {
            "type": "string",
            "title": "User ID",
            "description": "Your Winix user ID",
            "readonly": true
          },
          "refreshToken": {
            "type": "string",
            "title": "Refresh Token",
            "description": "The refresh token for your Winix account",
            "readonly": true
          }
        },
        "required": [
          "refreshToken",
          "username",
          "userId"
        ]
      }
    }
  },
  "layout": [
    {
      "type": "fieldset",
      "title": "Options",
      "description": "Configuration options for Winix Purifiers",
      "expandable": true,
      "expanded": true,
      "orderable": false,
      "items": [
        "exposeAirQuality",
        "exposeAmbientLight",
        "exposePlasmawave",
        "exposeAutoSwitch",
        "cacheIntervalSeconds",
        "deviceRefreshIntervalSeconds"
      ]
    },
    {
      "type": "fieldset",
      "title": "Authentication",
      "description": "Generated Winix credentials (read-only)",
      "expandable": true,
      "expanded": false,
      "orderable": false,
      "items": [
        "auth.username",
        "auth.userId",
        "auth.refreshToken"
      ]
    }
  ]
}
